name: Deploy to Firebase Hosting on merge
on:
  push:
    branches:
      - main
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # 👉 If you need to build Flutter web first, add this before deploy
      # - uses: subosito/flutter-action@v2
      #   with:
      #     channel: stable
      # - run: flutter build web --release && rm -rf public/* && cp -r build/web/* public/

      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_PAATASHAALA_1AD87 }}
          channelId: live
          projectId: paatashaala-1ad87
